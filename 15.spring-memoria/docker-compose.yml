name: spring-rag
services:
  db:
    image: qdrant/qdrant:latest
    ports:
      - '6333:6333'
      - '6334:6334'
    volumes:
      - /decimo/tools/qdrant_storage:/qdrant/storage
#        para que sea persistente la bd sino se pierde

volumes:
  qdrant_data:

